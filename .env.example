# Vite Development Server Settings (used by npm run dev proxy)
VITE_API_TARGET=http://localhost/umbrella_newversion

# Live Production API URL (used by npm run build to bake URL into compile JS bundle)
# Set this to your live backend API URL when building for production
VITE_API_URL=https://new-api.umbrellafinance.co/api
